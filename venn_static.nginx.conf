server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        autoindex on;

        # Cache static video/media files for 7 days
        add_header Cache-Control "public, max-age=604800";
    }
}
